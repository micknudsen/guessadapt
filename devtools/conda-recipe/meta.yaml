package:
  name: guessadapt
  version: '0.1'

source:
  git_rev: v0.1
  git_url: git@github.com:micknudsen/guessadapt.git

build:
  noarch: python
  script: "{{ python }} -m pip install . --no-deps -vv"
  number: 0

requirements:
  build:
    - biopython
    - python {{ python }}
    - setuptools
  run:
    - biopython
    - python
