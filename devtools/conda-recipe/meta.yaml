package:
  name: guessadapt
  version: '0.1'

source:
  git_rev: v0.1
  git_url: git@github.com:micknudsen/guessadapt.git

build:
  number: 0
  script: "{{ python }} -m pip install . --no-deps -vv"

requirements:
  build:
    - biopython
    - python {{ python }}
    - setuptools
  run:
    - biopython
    - python
